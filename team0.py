import random
def mySolution(history):
     number = random.randint(1,2)
     if number == 2:
          return "betray the sinner"              #choose to defect
     else:
          return "Cooperate with the POPO"              #choose to cooperate